import { TAttributes } from "@/types";

const att: TAttributes = {
  name: "Info Card",
  slug: "info-card.tsx",
  category: "Cards",
  dependencies: {
    external: ["framer-motion"],
    internal: [],
  },
};

export default att;
