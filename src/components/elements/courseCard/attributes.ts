import { TAttributes } from "@/types";

const att: TAttributes = {
  name: "Course Card",
  slug: "course-card.tsx",
  category: "Cards",
  dependencies: {
    external: ["framer-motion"],
    internal: [],
  },
};

export default att;
